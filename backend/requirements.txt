# ---- Core web framework ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0

# ---- Database ----
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2

# ---- Auth / security ----
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
passlib==1.7.4
python-multipart==0.0.9
itsdangerous==2.2.0

# ---- Validation / settings ----
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# ---- Rate limiting / caching ----
slowapi==0.1.9

# ---- NLP / AI engine (lightweight, CPU-only, no torch/GPU required) ----
nltk==3.9.1
scikit-learn==1.5.2
numpy==2.1.1
wordfreq==3.1.1
textstat==0.7.4
language-tool-python==2.8

# ---- Optional heavy models (transformer-based paraphrase/perplexity) ----
# Only imported if ENABLE_HEAVY_MODELS=true. Kept out of the default
# install path so the service stays lightweight and fully CPU-friendly
# on Render's free/starter instances.
# torch==2.4.1
# transformers==4.44.2
# sentencepiece==0.2.0

# ---- Utilities ----
httpx==0.27.2
python-dotenv==1.0.1
orjson==3.10.7
